name: CI-HelloWorld

on:
  push:
    branches:
      - CI-workflow
  workflow_dispatch:

jobs:

  hello-world-job:
    name: Hello World
    runs-on: 'ubuntu-latest'
    steps:
      - name: Run echo
        run: |
          echo "Hello world.!"

      - name: Run ls
        run: |
          ls -la
